# File Coverage Ledger — GhostWire V7 (HONEST / realistic status)
Excludes `.venv`, caches, `tests/`, `scratch/`.

> ## ✅✅✅ FULL-READ PASS COMPLETE (Rounds 1-46, MD ~1821 lines)
> Every non-test source file across agents/ · core/ · intelligence/(+waf_bypass/) · tools/ · utils/ · root/config has been read END-TO-END and logged. Priority 1-5 all done; the last ⬜ items (objective_ledger, engagement_recorder, logger, health_check, integration_test) were read in Rounds 38/42/43/46. The full read did NOT overturn the four-pathology diagnosis — it sharpened it, traced the corrupted-signal chain end-to-end to the user report (TOOLMGR-1 → EXPLOIT-EMIT-1 → SELFAWARE-OPSSANITY-BYPASS → OBJLEDGER-FALSE-WIN → REPORT-SPLIT-SUBSTR), and re-scoped Pathology 3 much smaller (auto_upgrader INERT, syntax_learner DEFENDED, EngagementRecorder/ToolInstaller/EvidenceRouter DEAD). Tier-1 fix crystallized: route EVERY trust decision through Evidence.is_proven(), never through the "vuln_proven"/confirmed_vulnerability substring/type. See ROOT_CAUSE_DEEP_ANALYSIS_2026-07-29.md Round 46 for the closing synthesis. Diagnosis-only; no code edited.


> ## ⏭️ TOMORROW'S TASK (full-read queue — do these end-to-end, then update ROOT_CAUSE_DEEP_ANALYSIS_2026-07-29.md)
> **Why:** partial/surveyed reads already gave two WRONG "dead" verdicts (smuggler/OOB/credential_finder were actually wired). Every 🟡 and 🟢 below was read for *key logic only*, not line-by-line — they must be fully read before any fixing. Verify each finding, catch missed wiring/bugs, and update the MD + this ledger.
>
> **Priority 1 — big decision files (most likely to hide more surprises):**
> 1. base_agent.py (5590, ✅ DONE) · 2. tool_manager.py (2090, ✅ DONE — Round 12) · 3. exploitation_agent.py (2368, ✅ DONE — Round 13) · 4. recon_agent.py (1954, ✅ DONE — Round 14) · 5. capability_registry.py (1225, ✅ DONE — Round 15) · 6. ai_backend.py (1321, ✅ DONE — Round 16) · 7. orchestrator.py (1058, ✅ DONE — Round 17) · 8. result_contracts.py (800, ✅ DONE — Round 18) · 9. state_store.py (737, ✅ DONE — Round 19)
>
> **✅✅ ALL 9 PRIORITY-1 FILES FULLY READ (Rounds 11-19). ~15,300 LOC of decision-critical code now read line-by-line.** Next: Priority 2 (WAF cluster + wired intelligence).
>
> **Priority 2 — WAF cluster + wired intelligence (survey gave wrong calls here — read carefully):**
> waf_ghost_engine.py (728, ✅ R20) · waf_fingerprinter.py (663, ✅ R24) · waf_learner.py (517, ✅ R23) · hypothesis_engine.py (478, ✅ R21) · ← WAF/hypothesis cluster DONE. Remaining: strategic_advisor.py (610, ✅ R25) · evidence_router.py (444, ✅ R26) · rule_generator.py (436, ✅ R27) · heuristic_optimizer.py (380, ✅ R28) · tool_success_tracker.py (269, ✅ R28) · engagement_analyzer.py (564, ✅ R29) · reasoning_engine.py (368, ✅ R30) · target_profiler.py (365, ✅ R31) · self_awareness_module.py (387, ✅ R31) · syntax_learner.py (201, ✅ R32) · structured_analyzer.py (241, ✅ R33) · attack_frontier.py (214, ✅ R33) · authz_tester.py (179, ✅ R33) · origin_discovery.py (496, ✅ R34) · cache_deception.py (186, ✅ R34) · tls_fingerprint.py (143, ✅ R34) ✅✅ PRIORITY 2 DONE. NEXT → Priority 3 (agents + plumbing)
>
> **Priority 3 — agents + core plumbing:**
> weaponization_agent.py (633) · reporting_agent.py (756, 🟢) · persistence_agent.py (225) · planning/objectives/specialists/mentor · target_graph.py (483) · tool_installer.py (631) · ip_rotator.py (357) · waf_ghost_engine · ssh_executor.py (338) · vps_optimizer.py (290) · target_context.py (215) · app_session.py (240) · tripwire_detector.py (190) · session/config_manager/unified_config_loader
>
> **Priority 4 — NEVER OPENED (⬜) — read these for sure:**
> objective_ledger.py (179) · engagement_recorder.py (163) · logger.py (119) · health_check.py (215) · integration_test.py (250)
>
> **Priority 5 — utils/config/tools:**
> tool_registry.py (409) · guardian.py (347, ✅ DONE — Round 22) · poc_templates.py (576) · poc_customizer.py (446) · display.py (515) · main.py (580) · config.py (303) · config_thresholds.py (170)
>
> **Definitely-done (✅ FULL, skip):** safe_executor, robust_parser, scope_enforcer, payload_sandbox, attack_graph, wsl_executor, sanitizer, validator, message_bus, url_utils, hil_manager, output_parser, config_paths, config_backends, cve_database, waf_bypass_orchestrator, waf_evasion_engine, waf_attacker, c2_simulator/obfuscator/evasion_controller (dead-but-read), installers/ast_analyzer/stealth (stubs).
> **Confirmed DEAD (skip, ~1,580 LOC):** browser_driver, stealth_proxy, waf_policy, constraint_engine, finding_scorer, engagement_learner, hardcore_evasion + 3 others per the dead list.


**Legend (honest about depth):**
- ✅ **FULL** — read end-to-end; small enough that I actually saw every line.
- 🟢 **CORE** — large file; read the decision-critical logic thoroughly, but NOT all lines (there are unread stretches).
- 🟡 **SURVEYED** — grepped the structure + read the key methods only. Defects found are real, but this is NOT exhaustive line-by-line reading.
- ⬜ **NOT READ** — not opened (or only its name/size seen).
- 💀 **DEAD** — confirmed 0 non-test imports; not read in full because it never executes.

> ~~Honest summary (STALE — pre-full-read): ~6,000 read line-by-line, ~9,000 surveyed, ~900 not opened; five biggest files 🟢/🟡...~~
> **UPDATED (post full-read pass, Rounds 1-46):** every non-test source file — including the five biggest (base_agent, tool_manager, exploitation, recon, ai_backend) and all previously-🟡/⬜ files — has now been read END-TO-END. The earlier "~15,000 LOC surveyed/not-opened" gap is CLOSED. Remaining unread = only `tests/` and the confirmed-💀 dead modules (skimmed, 0 non-test imports). See the ✅✅✅ banner at top.

## agents/
- ✅ base_agent.py (5590) — FULL decision-logic read (Round 11): init/wiring, phase gates, findings/severity/ops-sanity, liveness, command cleaning, ReAct action→command, full repair loop, _ai_repair_tool, think()/advice/assumptions. Found WAF-LEARN-1, WAF-EVA-4, CAPREG-ATTR, DEAD-EXPR; CORRECTED self_awareness/advisor/DEEP-4/PARSER-1/RC-3. (A few pure-helper stretches — wordlist-gen internals, canonicalize body, syntax-guide text — skimmed; no decision impact.)
- ✅ exploitation_agent.py (2368) — FULL read (Round 13): run() multi-scope orchestration, `_exploit_single_target` (port-probe→hypothesis→Phase-B ReAct loop→confirmed-vuln state machine→SSH brute), both finding paths. Found EXPLOIT-EMIT-1 (substring branch mints unproven VULN_PROVEN/critical → poisons confirmed-vuln gate), EXPLOIT-EMIT-2 (unbounded fire-and-forget sqlmap), EXPLOIT-SSH-1 (hardcoded absent metasploit wordlists), EXPLOIT-PORT-1/FP-1 (low). CORRECTED: the confirmed-vuln gate + `_test_hypotheses` differential are the BEST-gated logic in the engine — the plausibility gate the survey said was missing exists HERE.
- ✅ recon_agent.py (1954) — FULL read (Round 14): emit body + honeypot pruning, run() orchestration, `_run_recon_for_target` spine (WAF/baseline, evidence-gated ReAct loop, subdomain backstop, Tor ops, action-emit, bundle). Found RECON-EMIT-UNGATED (parses failed-tool output), RECON-SENSITIVE-1 (substring→critical mirror of EXPLOIT-EMIT-1), RECON-WAF-BASELINE (size filter lost under Tor), RECON-IDLE/WAF-IDX/SUBSEV (low). MAJOR CORRECTION: `waf_baseline_body` fix (l.855) makes the differential success-gate REAL (was fed a number, always auto-passed) — validates the whole Tier-1 plan. Strong deterministic backstops: tripwire pruning, evidence-gated stages, subdomain backstop.
- ✅ reporting_agent.py (756) — FULL read (Round 41, CAPSTONE): run()/_split_two_tier/_engagement_verdict/FIX-E demotion/_export_pocs/exec+tech prompts/markdown assembly. REPORT-SPLIT-SUBSTR (HIGH, capstone): _split_two_tier (l.623-626) classifies PROVEN on type=="confirmed_vulnerability" OR "vuln_proven" substring — NOT on Evidence object → EXPLOIT-EMIT-1/weapon-substring false positives reach the report as PROVEN → drive COMPROMISED verdict + headline critical counts + exec summary + survive FIX-E (l.66 exempts proven_vulns) → user sees "COMPROMISED, N critical proven." Completes the Pathology-2 chain end-to-end to the user report. Ops-sanity/FIX-E demote only findings self-tagged "unverified lead"/"[demoted" — but VULN_PROVEN bypasses ops-sanity, so they're never tagged. STRONG DESIGN (keep, just re-key on Evidence): two-tier split, honest objective verdict (surfaces OBJLEDGER-FALSE-WIN achieved objectives), FIX-E demotion, Evidence-backed PoC export (already loads evidence_objects — the fix infra exists!), defensive anti-hallucination prompts (forbid invent, forbid XXE-from-<!DOCTYPE>). Confirms inert learning tail at call-site (auto_upgrade dry_run=False R28, engagement_analyzer archival R29, waf_learner R23). Fix = classify PROVEN on Evidence.is_proven, not substring.
- ✅ weaponization_agent.py (633) — FULL read (Round 36): run() finding-filter, _synthesize_and_execute_poc (template + AI-PoC paths), proof validation, _is_false_positive, _generate_standard_payloads. STRENGTHS: AI-PoC prompt demands real proof (per-class indicators, baseline-compare, asserts, VULN_PROVEN/NOT_PROVEN protocol), solid _is_false_positive (WAF/framework/HTML-for-data guards), prior-FP-aware finding filter (skips SMUGGLER-FALSEPOS junk), guardian pre-exec validate_python. WEAPON-SUBSTR-PROOF (LOW-MED): elif "root:x:0"/"SQL syntax" branches (l.487-492) mark proven from raw substring, BYPASSING _is_false_positive → mints VULN_PROVEN (feeds confirmed-gate + dodges ops-sanity). WEAPON-NUCLEI-CONFIRM (MED): a finding whose active PoC FAILED is still promoted to proven_exploit/nuclei_confirmed if it's a nuclei "vulnerability" med+ severity (l.536-550) — manufactures confirmation from scanner say-so, no differential. Fix = accept confirmation only from real proof, not substring/scanner-match.
- ✅ persistence_agent.py (225) — FULL read (Round 37): PERSIST-FOOTHOLD-GATE (STRENGTH, enforced — hard-skips host-level probes unless _has_target_foothold; 2nd enforced anti-self-deception gate w/ ops-sanity); PERSIST-LOCAL-EXEC (MED residual — post-gate probes still run on scanner box (no target executor), can emit high-sev findings about local box if foothold finding was unproven; agent self-aware, blocks credential-SSH). Gate trusts a finding not a proof object (Tier-1).
- ✅ validation_agent.py (144) — FULL read (Round 37): validate_python (AST+AI-repair+SafePayloadValidator) + validate_bash (danger-deny + FIX C hallucinated-wordlist rewrite + FIX D concurrency caps + timeout injection). Distinct layer from utils/guardian (code-safety vs tool-allowlist). Solid defensive hardening, no defect.
- ✅ planning_agent.py (101) — FULL read (Round 37): Phase-1 ConOps/RoE gen, clean. (NOT the WAFFP-PLANNING-OVERRIDE writer — that's elsewhere.)
- ✅ objectives_agent.py (91) — FULL read (Round 37): clean, good severity discipline (never auto-escalate to critical, Nikto-noise filter), credential-SSH blocked. Effectively theoretical (no target executor).
- ✅ specialists.py (129) — FULL read (Round 37): Recon/Exploit/Research sub-agents, prompt-building from TOOL_REGISTRY+learned syntax → run_react. Clean. Non-stealth aggressive-by-default (design choice).
- ✅ mentor_agent.py (58) — FULL read (Round 37): StrategicMentor.advise() = transcript-level pivot recommendation (advisory, counterpart to reasoning_engine.analyze_tool_failure). Clean.

## core/
- ✅ safe_executor.py (87) · robust_parser.py (326) · scope_enforcer.py (138) · payload_sandbox.py (144) · attack_graph.py (52) · wsl_executor.py (618) · message_bus.py (88, ~read) · c2_simulator.py (26) · obfuscator.py (15) · evasion_controller.py (31) · url_utils.py (94) · hil_manager.py (23)
- ✅ ai_backend.py (1321) — FULL read (Round 16): GroqKeyPool rotation, query() contract, `_query_groq_with_rotation` TPD/RPM/auth/503 state machine, recovery/budget. MAJOR CORRECTION: backend NEVER hallucinates on exhaustion — returns validated non-empty content or RAISES RuntimeError; None on total groq failure. Retires the "exhaustion → hallucination" hypothesis → the real hallucination root is Pathology 2 (model reasoning from poisoned context). Found AIBACKEND-SLEEP-1 (query() can block ~3600s internally, bypassing callers' 300s cap = the real "engine goes unresponsive" symptom). Best-engineered file in the tree (same-key 8B rescue fixes a documented all-keys-burn bug).
- ✅ orchestrator.py (1058) — FULL read (Round 17): __init__ wiring, _phase_applicable (FENCED adaptive skip), full run() phase loop (4 fail-open gates, health kill-switch, checkpointing, asyncio.timeout, boundary validation, per-phase fault isolation w/ ExceptionGroup unwrap, finally teardown). Found ORCH-TIMEOUT-1 (asyncio.timeout CANNOT preempt the synchronous time.sleep inside ai.query() → AIBACKEND-SLEEP-1 defeats the phase deadline = the real "engine hangs" mechanism). Confirmed CAPREG-FRAG at wiring level (one cap_reg + separate ToolManager). Robust, config-driven (rules/orchestration.json).
- ✅ config_loader.py (200) — FULL read (Round 44): YAML loader (path-traversal guard + lru_cache), get_config 4-arg + ConfigManager. Clean. · 🟢 health_monitor.py (70)
- ✅ state_store.py (737) — FULL read (Round 19): background writer thread + WAL, schema, add_finding/get_all_findings, set_phase_data/get_phase_data merge, close(). The LAST finding-loss point in the data path. Found STATE-DEDUP-120 (authoritative dedup drops on 120-char prefix — tighter than base_agent's 160), STATE-MERGE-RACE (non-atomic read-modify-write loses phase_data updates under recon's multi-write), STATE-1 confirmed (10s write timeout aborts caller + feeds idle-loop undercount), STATE-CLOSE-2S (2s join risks final report writes). Consistency-correct core, leaky edges.
- ✅ result_contracts.py (800) — FULL read (Round 18): ToolResult success/validate, PhaseResult, ResultValidator/enforce_boundary, FragileParseFixer, TargetModel + ReconBundle. KEY: this is the DEFINITIONAL home of Pathology 2 — `success` = `status ∈ {SUCCESS,FALLBACK_SUCCESS,PARTIAL}` (structural), `validate()` checks only structural consistency (success⟺exit0, success⟹has-output), NEVER semantic result-presence. Banner-nmap passes validate() cleanly. The Tier-1 fix (per-capability expected-result predicate) belongs HERE or in the tool_manager classifier — one seam fixes every `if r.success` consumer. PARTIAL-counts-as-success is correct/load-bearing (don't tighten it).
- ✅ capability_registry.py (1225) — FULL read (Round 15): ~790 LOC declarative ToolCapability data + full CapabilityRegistry class (resolve/_is_installed/_install_tool/_detect_os/discover_custom_tool/list_all_tool_binaries/singleton) + cross-ref of all resolve() callers. KEY: this is a 2nd tool registry used ONLY by specialist/mentor sub-agents (main phase agents use TOOL_REGISTRY via tool_manager) = the concrete shape of Pathology 4. Found CAPREG-FRAG-2 (RESOLVED Round 22 → GUARDIAN-ALLOWLIST-1: guardian allowlist is NOT derived from ALL_TOOLS, it's a hand-authored 3rd registry that blocks novel tools on the main path), CAPREG-GATES-DEAD (can_auto_install/min_disk_mb/min_ram_mb/sha256 declared but `_install_tool` enforces none), CAPREG-INSTALL-DUP (60s vs 300s failed-cache), CAPREG-OS-DUP/RESOLVE-FIRSTWINS (low). NOT dead — well-built parallel stack.
- ✅ tool_installer.py (631) — FULL read (Round 42): 7-gate installer (registry/hard-block/scope/resource/trusted-source/install/verify + sha256 + session cap). TOOLINSTALL-DEAD (HIGH-P4/MED-safety): request_install + get_runtime_allowlist have NO production caller (tests only) → entire 7-gate safety path is DEAD; production installs via tool_manager engine bypass ALL gates (trusted-source, hard-block-binaries, SHA256 integrity [the ONLY sha256 check in the codebase → refines CAPREG-GATES-DEAD: sha256 effectively NEVER enforced], resource pre-flight, session cap). Confirms GUARDIAN-ALLOWLIST-1 STANDS (get_runtime_allowlist "convenience for guardian.py" never wired → guardian uses only hardcoded ALLOWED_RECON_TOOLS). 3rd dead install path + 3rd unreconciled allowlist = deepens Pathology 4. ~631 LOC dead. IRONY: the dead code is the GOOD (safety-first) code; fix = wire request_install (derived Gate-1 registry) + guardian consult get_runtime_allowlist → closes safety gap + P4 fragmentation at once. _verify_install checks binary-runs (NOT TOOLMGR-1 class).
- ✅ waf_ghost_engine.py (728) — FULL read (Round 20): transform() reactive gate + adaptive level calibration + per-tool injection, _mutate_payloads, feedback/get_block_rate, solve_challenge. CORRECTION: evasion is REACTIVE (clean-first, mutate-on-evidence, raw-socket-skip) — refutes the "force-everything-through-evasion" cascade framing. Found WAFGHOST-STATELESS (per-instance _feedback_stats defeated by TOOLMGR-3 fresh-per-retry), WAFGHOST-KWBREAK/EQLIKE (dubious but evidence-gated). solve_challenge = REAL Playwright CAPTCHA solver (not dead, independent of dead browser_driver); TLS/JA3 impersonation + origin-swap wired.
- ✅ tripwire_detector.py (190) — FULL read (Round 38): is_honeypot_active/check_banner_similarity/prune_honeypot_ports/_verify_live_http_service. Solid honeypot-artifact mitigation (the mechanism behind recon's Tor-all-ports-open pruning; pairs w/ ops-sanity + target_profiler port-low-trust = 3 defenses). TRIPWIRE-WEB-ONLY (LOW, intended trade-off, closes MD's "tripwire false-positive" check: prunes ALL non-web ports under honeypot suspicion → real multi-service host could lose SSH/DB). No real defect.
- ✅ ssh_executor.py (338) · app_session.py (240) · target_context.py (215) — FULL reads (Round 39), all CLEAN infrastructure. ssh_executor: solid VPS exec + _workload_active stall detector (CPU+socket churn → don't kill slow-but-working scanners; bounds TOOL exec only, not AIBACKEND-SLEEP). app_session: real auth session (cookies/bearer/CSRF), clone_anonymous=control arm for AuthzTester differentials; optimistic authenticated flag but nothing confirmed from it alone. target_context: careful URL parse w/ documented dup-scheme fix; add_subdomain auto-scopes (scope_enforcer is the real gate). No defects.
- ✅ ip_rotator.py (358) — FULL read (Round 40): Tor rotation, ensure_tor_ready (_tor_disabled fail-fast), NEWNYM, build_proxychains_cmd. CLOSES MD-l.488 check: build_proxychains_cmd carefully does NOT split literal `|` inside args (re-joins non-command segments) → repair transform does NOT corrupt valid commands. Tor-down → safe-bypass (unmodified cmd) = reinforces egress-cascade refutation. Clean.
- ✅ vps_optimizer.py (290) — FULL read (Round 40): VPS-OPT-STUBBED (LOW — all 4 sysctl/SSH/TCP/BBR tuning steps are no-op "Skipped (WSL)"; only dir-prep + scoped result-cleanup + disk-check run; over-reports "N changes applied"). Leftover H3 hypothesis debug-logging scaffolding. No real defect; cleanup uses scoped find|xargs (safe).
- ✅ target_graph.py (483) — FULL read (Round 43): TargetNode/Edge, add_target/relationship, analyze() correlation passes (shared IP/cert-SAN/NS/MX/subdomain), register_credential_test/pivot, get_pivot_candidates/shared_infra_clusters. SOUND + wired (exploitation:402-480 cred-reuse/pivot). Notably NOT corrupted-signal-driven: credential_tested/pivot_path edges reflect REAL auth-test outcomes → trustworthy by construction, does NOT participate in Pathology 2. Only limit = starved by thin/corrupted recon (input, not defect). No own-logic bug.
- ✅ session.py (78) · config_manager.py (34) · unified_config_loader.py (52) — FULL reads (Round 44, with config.py/config_thresholds/config_loader). All functional/clean. session.py CONFIRMS Round 28: results_dir=RESULTS_DIR/engagement_id (per-engagement) → tracker tool_metrics.json within-run-only + auto_upgrader repo-root write is a different file (path-split proven). config.py good safety defaults (blocked RFC1918/major-infra, consent, AUTO_APPROVE_INSTALLS=True). CONFIG-FRAG (LOW): 7 overlapping config modules, TWO clashing get_config signatures (config_loader 4-arg value vs config_manager no-arg MockConfig), two partial "unified" loaders — organizational echo of Pathology 4, cleanup-only.
- 💀 browser_driver.py (228) · stealth_proxy.py (124) · waf_policy.py (10)

## intelligence/
- ✅ cve_database.py (124, core read)
- 🟢 auto_upgrader.py (499, apply-path fully traced R27-28 → AUTOUPGRADE-FULLY-INERT) · ✅ engagement_analyzer.py (564) — FULL read (Round 29): analyze_engagement + all _analyze_* + save_insights. LEARN-1 pinned at ORIGIN (tool_rates.success_rate = status=="success", l.107/276 = structural signal). CORRECT reader of tool_runs SQL table (l.97/268 via get_tool_runs) — corroborates WAFLEARN-BATCH-DEAD fix direction. Trace CLOSED: only callers are auto_upgrader:122 (inert) + reporting_agent:187 (save_insights → archived JSON) = analysis-and-archive, poison never re-enters runtime. Loose WAF-type-from-"403"-substring (l.256, inert). Enables SYNTHESIS: intelligence layer's learning is overwhelmingly INERT-or-ADVISORY — exactly ONE learner (hypothesis_engine) ENFORCES anything; all others discarded or injected as advisory prompt text = META-RC-0/Pathology-1 generalized. → Pathology 3 smaller than framed; dominant failure is Pathology 1+2 (poisoned context window, not durable state). · ✅ tool_success_tracker.py (269) — see Round 28 entry above
- ✅ waf_bypass_orchestrator.py (714) — FULL read: WAF-ORCH-1/2/3 + the GOOD `_validate_bypass_differential`; imports smuggler/OOB/credential_finder (so they're WIRED, not dead)
- ✅ waf_evasion_engine.py (452) — FULL read: WAF-EVA-1/2/3; declarative tactic engine, in-memory-only learning
- ✅ hypothesis_engine.py (478) — FULL read (Round 21): generate_hypotheses (researcher brain, testable+confirm-signal) + validate_result (the Tier-1 gate) + Evidence.is_proven (result_contracts l.660). VERIFIED: proof-mandatory, proof_type-aware (differential requires similarity<0.97; artifact/oob self-proving), severity-capped-by-impact, fails-closed-to-lead. THE strongest component; the Tier-1 fix = route ungated paths (EXPLOIT-EMIT-1/TOOLMGR-1/recon-SENSITIVE) THROUGH this. Residual soft spot: no-baseline case degrades to trusting AI-described differential (so recon's baseline_body fix is what made it enforcing).
- ✅ waf_learner.py (517) — FULL read (Round 23): learn_from_engagement, _analyze_tactic_effectiveness, atomic locked DB, both base_agent writers (l.3044/3249) + reporting_agent batch path (l.439). Found WAFLEARN-KEYSPLIT (HIGH): successes filed under literal "header_mutation" (l.3045), failures under str(dict) (l.3250) → keys never match → per-tactic success_rate structurally uncomputable (always ~1.0 for header_mutation, 0.0 for everything else); WAFLEARN-BATCH-DEAD (MED-HIGH): reporting path reads phase_data["tool_runs"] which NO writer populates (tool_runs live only in the SQL table via record_tool_run) → batch learner always gets [] → learns zero tactic effectiveness. LEARN-1 confirmed at source (success writer keys off structural ToolResult.success = Pathology 2). STRENGTHS (keep): atomic temp+fsync+replace DB, full RMW inside lock, parent-dir guard, Retry-After capture — file itself is well-built; bugs are all at the wiring seam. Supersedes/expands the old WAF-LEARN-1 note.
- ✅ strategic_advisor.py (610) — FULL read (Round 25): record_tool_outcome/finding/waf_detection, advise_* methods, should_continue_trying, record_engagement_outcome, save/load KB. Wiring confirms DEEP-2 (should_continue_trying + advise_waf_evasion DEAD; advice = soft prompt note). NEW: ADVISOR-NONATOMIC-SAVE (MED-HIGH — _save_knowledge_base l.73 direct truncate-write on EVERY record + _load swallows decode error → crash/concurrent write silently WIPES entire strategic KB; same corruption class waf_learner/fingerprinter FIXED but left unfixed here + O(n²) write amplification); ADVISOR-WAF-ALLTRUE (MED — record_engagement_outcome l.477 records every waf tactic success:True unconditionally); ADVISOR-SIGNAL (Pathology 2 — success counts on structural ToolResult.success); ADVISOR-STORE-FRAG (Pathology 4 learning layer — 4th unreconciled store). Minor: ADVISOR-PIVOT-LOGIC (dead method, mixed metrics). Design sound; persistence fragile + signal poisoned.
- ✅ evidence_router.py (444) — FULL read (Round 26): ROUTING_RULES table, route_finding/batch, TechStackRouter.route_tech_stack + _exploit_type_to_command + exploitation call-site (l.847). Found ROUTER-DEAD-EVIDENCEROUTER (~264 LOC: the entire EvidenceRouter class + ROUTING_RULES table has NO external caller — dead; only TechStackRouter is imported. Symbol-level dead code an import-census misses); ROUTER-CMD-DEMOTED (even live TechStackRouter's hardcoded curl/nuclei command strings + _exploit_type_to_command ~90 LOC are DISCARDED at the call-site — only description→known_lead seed kept, per docstring "minor seed, don't let it dominate" → hardcoded commands NEVER executed = CORRECT autonomy-preserving demotion, routed through hypothesis→validate gate; also means these /etc/passwd probes do NOT feed EXPLOIT-EMIT-1). ROUTER-SUBSTR-LOOSE (LOW, in dead class). CONCLUSION: the largest hardcoded exploit-knowledge block is largely INERT (half dead, half demoted to non-binding hints) — evidence FOR the exploitation path being genuinely autonomous, NOT a 2nd guardian-style hardcoding violation. Only action = cleanup.
- ✅ rule_generator.py (436) — FULL read (Round 27): generate_rules_from_insights + all _generate_* + merge_rules_to_system/_append_to_rule_file + auto_upgrader apply path. KEY: RULEGEN-MERGE-DEAD — merge_rules_to_system/_append_to_rule_file/save_rules have NO caller; auto_upgrader._apply_changes (dry_run=False at reporting:365) writes the rules_package only to a ROLLBACK-BACKUP file (generated_rules.json) that nothing reads back → the rule-rewriting feedback loop is OPEN/inert → generated (corrupt-signal-derived) rules CANNOT poison the live rules/*.json = SCOPING CORRECTION narrowing Pathology 3 for auto_upgrader to just tool_metrics.json. Downside: wasted compute + misleading "[AUTO-UPGRADE] Phase 3: rules generated" log (learning that isn't applied). RULEGEN-APPEND-LATENT (latent HIGH if ever wired: no dedup→unbounded dupes, reads dict-shape but writes bare list→corrupts rule file, confidence minted 0.9/0.95 > 0.8 threshold→toothless gate). Hardcoded endpoint/command/tool maps = inert (like evidence_router). ~90 LOC dead. Add to dead census.
- ✅ heuristic_optimizer.py (380) — FULL read (Round 28): optimize_from_insights (tool priority=success_rate + duration penalties, confidence bumps, timeout 1.5x, waf tool-avoid), apply_optimizations. HEUR-INERT (own apply is backup-only; output consumed ONLY by auto_upgrader's inert paths) + HEUR-SIGNAL (all adjustments downstream of corrupted signal — moot while inert). Pure compute wired to nothing live.
- ✅ tool_success_tracker.py (269) — FULL read (Round 28): log_tool_result (live, base_agent:1036), get_tool_effectiveness/rank_tools_for_target/profile_target (DEAD — no callers), summarize_effectiveness (→ prompt l.5190). TRACKER-DECISION-DEAD (ranking decisions dead; learning reaches engine only as advisory prompt summary = Pathology 1 shape, like advisor). TRACKER-SIGNAL (log_tool_result success = structural ToolResult.success). STRENGTHS: atomic save (temp+replace, the fix advisor lacks) + WAF-block EXCLUDED from denominator (l.132-135, valid_runs=total-waf_blocks = genuine signal-cleaning, the Tier-1 instinct applied at a learning input — replicate this). Caveat: db_path=session.results_dir → likely within-run only, so cross-run learning aspirational.
- 🚨 AUTOUPGRADE-FULLY-INERT (Round 28, MAJOR Pathology-3 scope correction): the ENTIRE auto_upgrader pipeline (rule_generator R27 + heuristic_optimizer + tool-metrics writer, ~1,600 LOC across 4 files) computes insights→optimizations→rules every engagement and writes them ONLY to rollback/backup files nothing reads. Tool-metrics write is inert on TWO axes: path (writes repo_root/tool_metrics.json, tracker reads results_dir/tool_metrics.json) AND schema (writes key "tool_effectiveness", tracker reads "tool_stats"). auto_upgrader does NOT close any runtime feedback loop → STRIKE it from Pathology 3's permanent-poisoning story. Real live poisoned loops = ToolSuccessTracker(record) + WafLearner + StrategicAdvisor + syntax_learner.
- ✅ reasoning_engine.py (368) — FULL read (Round 30): reason_about_findings, analyze_tool_failure, assess_confidence_calibration, calibration_summary_for_prompt + all 4 call-sites. MOST-WIRED intelligence module: closed loop (recon:1595 reason → exploitation:969 measure → base_agent:5379 adjust), calibration persisted cross-run. REASON-CALIBRATION-CLEAN (STRENGTH + correction): outcome fed to calibration = h.status=="confirmed" (exploitation:972) = the VALIDATED/proof-gated verdict, NOT structural success → this loop is IMMUNE to Pathology 2 = 2nd-best component after hypothesis_engine. REASON-ADVISORY (Pathology 1): all 3 outputs are advisory prompt-injection — analyze_tool_failure verdict→_recent_failures note (base_agent:1083, NOT a deterministic pivot/abandon; live counterpart to the dead should_continue_trying but as suggestion not gate), calibration→prompt note, reasoning→pre-analysis note. Even the BEST feedback only advises → cements Tier-1/META-RC-0 fix = enforcement not better learning. Minor: REASON-TRIGGER-EXACT3 (==3 exact, once-only). No-AI fallbacks sound.
- ✅ target_profiler.py (365) — FULL read (Round 31): build/_gather_evidence/_extract_api_routes (JS-bundle route mining)/_synthesize/format_for_prompt + call-sites (recon:1898 build, exploitation:2017 inject). Competent evidence-driven zero-hardcoding synthesis (plausible vs implausible vuln classes, port-scan-low-trust, safe fallback). TARGETPROFILER-ADVISORY (LOW-MED): the "IMPOSSIBLE — DO NOT ATTEMPT" strategic thesis is injected as PROMPT TEXT, not an enforced skip (Pathology 1). objective_ledger:77 also reads implausible_vuln_classes (2nd consumer — check at P4). No own-logic defects.
- ✅ self_awareness_module.py (387) — FULL read (Round 31): register_finding/knowledge_gap/assumption/tool_outcome (advisory recording), ops_sanity_check + base_agent call-site (l.1279). SELFAWARE-OPSSANITY-ENFORCED (corrects "all advisory"): ops_sanity_check IS enforced — _ops_sanity_backstop in add_finding (base_agent:1290-1298) DETERMINISTICALLY downgrades implausible findings to "info/UNVERIFIED LEAD" before storage (catches 1000+-ports-via-proxy artifact, ≥97%-baseline-identical "bypass", host-claims-without-foothold). One of the FEW enforced self-knowledge gates. SELFAWARE-OPSSANITY-BYPASS (MED): the backstop EARLY-EXITS when detail contains "VULN_PROVEN"/"Proof[" (base_agent:1265) = exactly the token EXPLOIT-EMIT-1 (R13) fabricates → forged VULN_PROVEN bypasses the one enforced anti-self-deception gate → advances to confirmed_vulns unchecked by BOTH the differential gate AND ops-sanity. Fix: exempt on a trusted proof OBJECT (Evidence.is_proven), not the substring. Recording side = advisory (Pathology 1); register_tool_outcome success = structural (Pathology 2). "empty=silent" report fix good.
- ✅ syntax_learner.py (201) — FULL read (Round 32): learn_syntax/_abstract/_rehydrate/_strip_workspace_wordlist/get_syntax_hints + wiring (base_agent:2991 learn, 3795/3896/4153 hints). RC-5 SUBSTANTIALLY REMEDIATED: input signal-cleaning (_HARD_FAIL_MARKERS refuses to learn the exact "nmap QUITTING!" garbage RC-5 cited, even if scored success) + portable target/workdir/wordlist abstraction (kills stale-host/dead-wordlist replay, self-heals poisoned entries) + atomic save + bounded 10/tool. One of the BETTER-defended learners. Residual: SYNTAXLEARN-BANNER-SLIP (LOW — banner-only nmap false-success has no fail marker so shape still learned; low harm, it's an abstracted reference hint); advisory delivery is CORRECT for syntax. → downgrades RC-5 from "permanent poison" to "remediated, minor residue." Combined with R28 (auto_upgrader INERT), BOTH of the MD's named "permanent corruption" loops are downgraded → Pathology 3 much smaller; dominant failure = Pathology 1+2.
- ✅ structured_analyzer.py (241) — FULL read (Round 33): analyze_raw_output/structure_recon_phase_output/_build_tactical_context (recon:1581 → reasoning_engine advisory). STRUCTURED-BETTER-SIGNAL: AI judges success/confidence BY READING OUTPUT (marks WAF-block/404 low-conf) = semantically grounded, closer to "real result" than structural classifier — engine already computes a better success signal here but doesn't gate on it (Tier-1 opportunity). No own-logic defect; non-deterministic + advisory.
- ✅ attack_frontier.py (214) — FULL read (Round 33): FrontierItem/effective/push/pop/record_outcome/_boost_neighbors/snapshot_for_prompt. Well-designed scored self-expanding queue (priority×novelty+momentum, no hardcoded value-keywords). FRONTIER-POPLOOP-DEAD (MED): agents call ONLY push_many + snapshot_for_prompt(top=15) (exploitation:1960-1997); pop/record_outcome/expand/momentum machinery exercised ONLY in tests → frontier degrades to a one-shot static ranked prompt list, adaptive loop dead, "subdomains-forgotten" fix only half-realized. Enforcement mechanism built, consumed advisorily. Fix: drive the loop off pop()+record_outcome().
- ✅ authz_tester.py (179) — FULL read (Round 33): test_bola/test_idor differential IDOR/BOLA + exploitation call-site (1019-1037). AUTHZ-WIRED-CLEAN (GOLD STANDARD): inherently differential (control owner vs test other-identity/neighbor-id; confirmed only on real body delta; proof_type=differential + high only on proof), wired, emits real Evidence(differential)→LEGIT VULN_PROVEN. Immune to Pathology 2 by construction. THE template for Tier-1 "trust the Evidence object not the substring" — same VULN_PROVEN token as EXPLOIT-EMIT-1's fabrication, indistinguishable downstream = why SELFAWARE-OPSSANITY-BYPASS + confirmed-vuln gate must key on Evidence.is_proven, not text. No own-logic defect.
- ✅ waf_fingerprinter.py (663) — FULL read (Round 24): fingerprint_target ~50 behavioral probes, _calculate_confidence strong-signal gate, _match_known_fingerprints, save_fingerprint + recon call-site (l.700-799). CORROBORATION: under Tor recon SKIPS the local probe entirely (recon:711-719, avoids IP leak) → fingerprinter physically cannot misread Tor-403s as WAF (3rd egress-cascade refutation); confidence gate requires payload/header strong signal (path-403/timing capped 0.2 < 0.5) = genuine anti-false-positive strength. Found WAFFP-PLANNING-OVERRIDE (MED-HIGH): recon:736-747 lets a planning-LLM recommended_bypass synthesize a fake fingerprint with hardcoded confidence 0.8, bypassing the strong-signal gate → waf_present=True on zero behavioral evidence (Pathology-1 at the planning seam). WAFFP-TYPE-MISNOMER (LOW-MED): fingerprinter never sets waf_type → recon:768 always uses detected_patterns[0] (a PATTERN name, not a WAF product) → becomes the learner's waf_id, compounding WAFLEARN-KEYSPLIT on the waf_id axis. Minor: WAFFP-PROXY-UNUSED (proxies plumbing dead at sole call-site), WAFFP-SIM-PARTIAL (numeric/str behaviors ignored in similarity denominator → known-match under-fires). File itself well-built (atomic save, time budget w/ strong tests first, conn-error≠block).
- ✅ objective_ledger.py (179) — FULL read (Round 38): update_from_findings/should_stop/momentum/_tailor_to_target + exploitation call-site (1720-1743). OBJLEDGER-FALSE-WIN (HIGH): objectives advance on (severity∈{high,critical} OR type) + SUBSTRING markers (l.91-107, no proof); should_stop is an ENFORCED break (exploitation:1739-1743) when no active primaries remain → a false-positive high finding (EXPLOIT-EMIT-1/recon-SENSITIVE/weapon-substring) marks an objective "achieved" → engine QUITS EARLY declaring false victory = Pathology 2 → premature termination (most user-visible "hallucinated success + stopped" symptom). Fix: advance only on Evidence proof object. GOOD: _tailor_to_target ENFORCES implausible-objective abandonment (partial correction to R31 — Target Model's IMPOSSIBLE list HAS an enforced consumer here); idle-streak exhaustion stop; respects min loops.
- ✅ engagement_recorder.py (163) — FULL read (Round 43): RECORDER-DEAD — no production caller (not even tests) → dead code. Its finalize_and_learn is the ONLY caller of advisor.record_engagement_outcome → so ADVISOR-WAF-ALLTRUE (R25) is a real bug on a DEAD path (refines R25; advisor's live poison = record_tool_outcome/finding via base_agent, ADVISOR-NONATOMIC-SAVE stands). Redundant unwired 2nd recording layer. Add to dead census.
- 💀 constraint_engine.py (275) · finding_scorer.py (264) · engagement_learner.py (50)

## intelligence/waf_bypass/
- ✅ waf_attacker.py (56)
- ✅ origin_discovery.py (496) — FULL read (Round 34): discover_origin_ips + all techniques + test_origin_connection + wiring (recon:1484, orchestrator:56/110/512/520). Real+wired but ~4/9 techniques STUBBED (ORIGIN-STUBS, expands ORIGIN-1: historical DNS/ASN/cloud-metadata/internal-probe are pass/placeholder). ORIGIN-CONF-DENOM (LOW: confidence=/9.0 counts removed stubs→understated, max~0.55). ORIGIN-CDN-LIST (LOW: 3-entry hardcoded CDN CIDR list→over-ranks not_cdn). Capability-completeness gap, not root-cause.
- ✅ cache_deception.py (186) — FULL read (Round 34): probe_deception/probe_poisoning + wiring (exploitation:1110-1134). GOLD-STANDARD differential probe: confirms only on same-dynamic-content-now-cached (sim≥0.85) or benign-canary-reflected-into-cacheable; non-destructive (unique canary, never persists). Immune to Pathology 2 by construction. No defect.
- ✅ tls_fingerprint.py (143) — FULL read (Round 34): curl_cffi/curl-impersonate browser-JA3, rewrite_curl_for_impersonation + wiring (waf_ghost_engine:161). Graceful no-op when tooling absent ("can only ever help"). No defect; effectiveness gated on provisioning. verify=False intentional.
- ✅✅ PRIORITY 2 COMPLETE (Rounds 20-34) — all WAF-cluster + wired-intelligence files fully read. Pattern: differential/proof modules (hypothesis_engine, authz_tester, cache_deception, ghost-engine gate, ops_sanity) are correct/Pathology-2-immune; learning modules mostly inert (auto_upgrader chain) or advisory (advisor/tracker/frontier-snapshot/syntax-hints); enforcement points undermined only by substring-trust (VULN_PROVEN text vs Evidence object). Building blocks for correct behavior exist + well-built; disease = trust routed through emitter-writable text + advisory prompts instead of proof objects + gates.
- ✅ request_smuggler.py (379) · oob_exfil_engine.py (377) · credential_finder.py (135) — FULL reads (Round 35), the WEAK side of the WAF-bypass set (executed by orchestrator _execute_*_bypass). CREDFINDER-NO-VALUE (finds header names, never values → create_bypass_request always refuses → non-functional, fails safe). SMUGGLER-FALSEPOS (MED: execute_smuggling_attack calls it "bypassed" on ANY non-403/429 response — no desync differential, smuggled_request_processed never set → high false-positive; +HTTP2 stub, confidence/4.0 understated). OOB-STUB (MED: payload CATALOG only — discover_oob_vectors hardcodes waf_bypassed:True, monitor_oob_channel is a stub (exfil_successful=False always), collaborator_domain="collaborator.oob" placeholder → _execute_oob_bypass asserts bypass via non-existent collaborator, no verification = EXPLOIT-EMIT-1 class one layer down). CONTRAST: gold-standard differential siblings (authz_tester/cache_deception/origin) PROVE; these ASSERT. orchestrator's _validate_bypass_differential (WAF-ORCH) is load-bearing for this weak group. Additional Pathology-2 loci confined to WAF-bypass exec layer.
- 💀 hardcore_evasion.py (554) — genuinely dead (not imported by orchestrator either).

## tools/
- 🟢 output_parser.py (303) — parsers + dispatch read.
- ✅ tool_manager.py (2090) — FULL read (Round 12): install engine (VPS+WSL AI-repair+PATH-locator), discover/learn-syntax, help-grounding, universal input-repair cluster, `_validate_and_fix_command`, `run`, `_execute` classifier. Found TOOLMGR-1 (banner-nmap → false SUCCESS = 2nd locus of corrupted success signal), TOOLMGR-2 (VPS transient-error never retried), TOOLMGR-3 (stateless WafGhostEngine per retry), TOOLMGR-4/-5 (low). CORRECTED: `validate_and_filter_flags` deliberately disabled (zero-hardcoding win), classifier + input-repair are strong.
- ✅ tool_registry.py (409) — FULL read (Round 45): TOOL_REGISTRY (~30 tools binary/install/category/timeout/syntax-constraint-desc) + TOOL_TIMEOUTS/VIRTUAL/HTTP/STREAMING/FALLBACKS. Well-maintained (GitHub-latest-release installs, useful hard constraints in descriptions). PATHOLOGY-4 THIRD REGISTRY confirmed at data level: ~30 tools here vs guardian ~90 (ALLOWED_RECON_TOOLS, live run-gate) vs capability_registry 44 (ALL_TOOLS) — differ in size/membership, none authoritative. No defect.
- ✅ installers.py (3, empty stub)

## utils/
- ✅ sanitizer.py (57) · validator.py (48) · ast_analyzer.py (3, stub) · stealth.py (6)
- ✅ poc_templates.py (576) · poc_customizer.py (446) — FULL reads (Round 45). poc_templates = EVIDENCE-BASED PoC skeletons (check real SQL-error/time-delay/canary-reflection, homepage guards, VULN_PROVEN/NOT_PROVEN protocol) = the reliable proof path (the RIGHT way to mint VULN_PROVEN, unlike substring paths). poc_customizer = recon-grounded PoC param customization (clean; waf_type always "unknown" per WAFFP-TYPE-MISNOMER). No defects.
- ✅ guardian.py (347) — FULL read (Round 22): ALLOWED_RECON_TOOLS set, validate_ai_command, BLOCKED/DESTRUCTIVE patterns, block_or_repair + base_agent l.2445 call-site. Found GUARDIAN-ALLOWLIST-1 (HIGH): the hardcoded ~90-tool ALLOWED_RECON_TOOLS set gates the MAIN path (base_agent:2445) → any novel AI-discovered/installed tool not in the static list is BLOCKED = directly contradicts the autonomous "go-anywhere" design + poisons feedback (policy-block read as tool-failure). This is the THIRD independent tool registry (guardian set vs cap_reg.ALL_TOOLS vs tool_manager.TOOL_REGISTRY) = definitive Pathology 4. RESOLVES CAPREG-FRAG-2: the allowlist is NOT derived from ALL_TOOLS — it's hand-authored. STRENGTH (keep): BLOCKED_PATTERNS / DESTRUCTIVE_PATTERNS are pattern-based, tool-agnostic, fail-closed HITL — the real safety rail, separable from the allowlist. Fix direction: derive the allowlist (union of registries + live installs), keep the danger-pattern gate. · poc_templates.py (576) · poc_customizer.py (446) · display.py (515, low-risk render, skimmed)
- ✅ logger.py (119) — FULL read (Round 46): Rich handler + RotatingFileHandler (10MB×5, bounded) + UTF-8. Clean.

## root / config
- ✅ main.py (580) — FULL read (Round 46): CLI→session→orchestrator, UTF-8/console setup, readiness ping, legal/consent gate. Wiring only, no decision defect.
- ✅ display.py (515) — FULL read (Round 46): Rich UI rendering (banner/section/tables/legal/briefing). Presentation-only, no finding/proof logic.
- ✅ health_check.py (215) · integration_test.py (250) — FULL reads (Round 46): standalone dev/CI diagnostic scripts (py_compile/import/pipeline tests), NOT engine runtime. No decision code.
- ✅ config_paths.py (271) · config_backends.py (92)
- ✅ config.py (303) · config_thresholds.py (170) — FULL reads (Round 44). Clean config; good safety defaults; CONFIG-FRAG noted.
- (health_check.py + integration_test.py — ✅ FULL read Round 46, see agents/ section entry; standalone diagnostic scripts.)

---
## Honest bottom line
- **What I can stand behind fully:** ~15 small files read end-to-end; the *decision-critical logic* of the big files (react loop, classifier, repair, learning loops, provisioning, state writer, parsers, sanitizer) — that's where all ~40 defects live and they are solid.
- **The real remaining gap:** ~3,700 unread lines of `base_agent.py`, ~1,500 of `tool_manager.py`, ~1,000 of `capability_registry.py`, most of `state_store` query methods and `result_contracts`, the impl of `target_graph`, and ~5 unopened small files (`objective_ledger`, `engagement_recorder`, `logger`, `health_check`, `integration_test`). New defects *could* still hide in those stretches — but they are unlikely to change the four-pathology diagnosis (they're helpers/queries/plumbing around the cores already analyzed).
- **Dead code — CORRECTED to ~1,580 LOC across 10 modules** after the full read of `waf_bypass_orchestrator.py`. `request_smuggler` (379), `oob_exfil_engine` (377), and `credential_finder` (135) were wrongly counted dead — they are instantiated by the orchestrator (`:50-60`), which is wired into exploitation. Removed ~891 LOC from the dead total. The earlier import-census only searched `agents/`+`core/` and missed intelligence-internal imports; lesson: survey-based "dead" verdicts need an all-directory grep before being trusted.
