# Requires Python 3.12+ (the codebase uses PEP 701 multi-line f-strings, a
# SyntaxError on 3.11 and earlier). On Ubuntu 22.04/Debian (ships 3.10): install
# python3.12 via deadsnakes and create the venv with it. main.py enforces this.
rich>=13.7.0
requests>=2.31.0
python-dotenv>=1.0.0
ollama>=0.2.0
groq>=0.9.0
openai>=1.30.0
pyyaml>=6.0.1
jinja2>=3.1.4
ipaddress
dnspython>=2.6.1
paramiko
