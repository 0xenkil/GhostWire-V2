"import json\nimport re\n\nlog_path = r\"C:\\Users\\ASUS\\.gemini\\antigravity\\brain\\9192f495-4fd4-4963-a820-51a956d44cda\\.system_generated\\logs\\transcript.jsonl\"\n\nfound_versions = []\n\nwith open(log_path, \"r\", encoding=\"utf-8\") as f:\n    for line_idx, line in enumerate(f):\n        try:\n            data = json.loads(line)\n            # Look for replace_file_content or view_file tool calls\n            tool_calls = data.get(\"tool_calls\", [])\n            for tc in tool_calls:\n                args = tc.get(\"Arguments\", {})\n                if isinstance(args, str):\n                    try:\n                        args = json.loads(args)\n                    except:\n                        pass\n                \n                # Check if it targets display.py\n                if isinstance(args, dict):\n                    target = args.get(\"TargetFile\") or args.get(\"AbsolutePath\") or \"\"\n                    if \"display.py\" in target:\n                        # Check if it has content\n                        content = args.get(\"ReplacementContent\") or args.get(\"CodeContent\")\n                        if content and len(content) > 1000:\n                            found_versions.append((line_idx, \"write\", content))\n                            \n            # Check system outputs (views/diffs)\n            content_str = data.get(\"content\", \"\")\n            if \"utils/display.py\" in content_str and \"PHASES = {\" in content_str:\n                found_versions.append((line_idx, \"content\", content_str))\n        except Exception as e:\n            pass\n\nprint(f\"Found {len(found_versions)} potential versions.\")\nif found_versions:\n    # Print the last one found\n    last_ver = found_versions[-1]\n    print(f\"Last version index: {last_ver[0]} type: {last_ver[1]}\")\n    # Write it to scratch/recovered.txt\n    with open(\"scratch/recovered.txt\", \"w\", encoding=\"utf-8\") as out:\n        out.write(last_ver[2])\n    print(\"Wrote last version to scratch/recovered.tx
<truncated 49 bytes>